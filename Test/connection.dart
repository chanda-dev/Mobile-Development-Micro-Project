class Connection {
  final String? host;
  final String? username;
  final String? password;
  final int? port;
  final String? dbname;

  Connection(this.host, this.username, this.password, this.port, this.dbname,);
}